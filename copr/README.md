Build order:

 * rust-is_terminal_polyfill
 * rust-dma-heap

Notes:

 * rust-dma-heap carries a metadata patch that adds SPDX license metadata to
   Cargo.toml so Fedora RPM Rust macros can process the crate correctly.
